#include <fstream>
#include <iostream>
#include <vector>
#include <random>
#include <cmath>
#include <string>

const int MAX_VERTICES = 500; // max количество вершин
const double K = 0.1; // коэффициент отталкивания
const double L = 100.0;  // расстояние между вершинами
const double EPS = 1e-5; // сравнение с погрешностью
